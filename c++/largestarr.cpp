#include<iostream>
#include<cmath>
int main(){
       int num[]={};
       std::cout << "enter the array size:";
       std::cin >> num;
       for(int nums : num){
	       emp_num=nums
           for(int i=0,i<sizeof(num),i++){
		  if(emp_num > num[i]){
			  std::cout << "elment" << emp_num << "is a largest num"; }
		  else if(emp_num==num[i]){
			  continue;
		  }
		  else if(){
		  }
		  else{
		  }
	     }}
        return 0;
}
